---
import { ScanLine, Code, ShieldCheck, CheckCircle2 } from 'lucide-react';

const steps = [
    { id: "01", title: "Discovery", desc: "We don't write a single line of code until we understand your business model. We deliver a full technical spec.", icon: ScanLine },
    { id: "02", title: "Development", desc: "Two-week sprints. You get a deployable link every Friday. No black boxes, just visible progress.", icon: Code },
    { id: "03", title: "Testing", desc: "Automated E2E testing pipelines. If it breaks, it breaks on our machines, not in production.", icon: ShieldCheck },
    { id: "04", title: "Handoff", desc: "We hand over the keys, the documentation, and the training recordings. You own the IP completely.", icon: CheckCircle2 }
];
---

<div class="grid grid-cols-1 md:grid-cols-2 gap-8 md:gap-12">
    <div class="md:sticky md:top-32 h-fit mb-4 md:mb-0">
        <h3 class="text-3xl font-bold text-white mb-6">How we work.</h3>
        <p class="text-zinc-400 text-lg leading-relaxed mb-8">
            Software projects fail because of communication, not technology. We solved this with a rigid, transparent process.
        </p>
        <a href="/playbook" class="inline-block px-6 py-2.5 bg-zinc-100 text-black hover:bg-white rounded-md text-sm font-medium transition-colors">
            Read our Playbook
        </a>
    </div>
    <div class="flex flex-col gap-0 border-l border-zinc-800 pl-8 md:pl-12">
        {steps.map((s, i) => (
            <div class="relative py-8 md:py-12 group methodology-step">
                <div class="absolute -left-[37px] md:-left-[55px] top-8 md:top-12 w-3 h-3 md:w-4 md:h-4 rounded-full bg-zinc-900 border border-zinc-700 group-hover:border-white group-hover:bg-white transition-colors z-10"></div>
                <div class="mb-2 flex items-center gap-3">
                    <span class="font-mono text-xs text-zinc-600">0{i+1}</span>
                    <h4 class="text-xl font-bold text-white">{s.title}</h4>
                </div>
                <p class="text-zinc-400 text-sm leading-relaxed max-w-md">{s.desc}</p>
            </div>
        ))}
    </div>
</div>
